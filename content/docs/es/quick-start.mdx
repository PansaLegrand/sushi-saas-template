---
title: Inicio rápido
icon: "Zap"
description: Ejecuta la plantilla Sushi SaaS en local con pnpm, recorre rutas i18n, health checks y blogs MDX, y aprende dónde configurar autenticación, pagos y documentación.
keywords:
  - inicio rápido
  - configuración
  - Next.js
  - pnpm
  - i18n
  - MDX
  - health check
tags: [setup, i18n, MDX]
author: "Pansa Legrand"
publishedAt: "2025-09-29T00:00:00.000Z"
updatedAt: "2025-09-29T00:00:00.000Z"
canonical: "https://sushi-saas-template.io/es/blogs/quick-start"
---

## Desarrollo local

Requisitos: Node 20+ y pnpm 9+.

```bash
git clone https://github.com/PansaLegrand/saas-sushi-template.git saas-sushi-template
cd saas-sushi-template
pnpm install
pnpm dev
```

Rutas útiles:

- Landing: `/en`, `/zh`, `/es`, `/fr`, `/ja`
- Salud: `/api/health` (responde `{ status: "ok" }`)
- Docs (MDX): `/:locale/blogs/quick-start`

---

## Internacionalización (next-intl)

- Textos en `messages/*.json`
- Locales en `src/i18n/locale.ts` (configurar `locales`, `defaultLocale`, `localePrefix`)
- Middleware en `src/middleware.ts` (prefijo `/:locale/...`)
- Carga de mensajes en `src/i18n/request.ts`

Añadir un idioma:

1) Crear `messages/<locale>.json`
2) Añadir el código a `locales` en `src/i18n/locale.ts`
3) Reiniciar el servidor

---

## Docs con MDX (Fumadocs)

Los archivos viven en `content/docs/<locale>/...` y se sirven en `/:locale/blogs/<slugs>`.

- El script de desarrollo genera `.source/index.ts` automáticamente
- El plugin de Fumadocs en `next.config.ts` analiza MDX y frontmatter

Crear una página:

```bash
mkdir -p content/docs/es
echo "---\ntitle: Mi página\n---\n\n# Hola" > content/docs/es/my-page.mdx
```

Visita `/es/blogs/my-page`.

---

## Autenticación (Better Auth)

- Configuración del servidor en `src/lib/auth.ts` con el handler
  `/api/auth/[...all]/route.ts`
- Cliente y hooks en `src/lib/auth-client.ts` (`/[locale]/login`, `signup`, `me`)
- Define `BETTER_AUTH_SECRET`, `BETTER_AUTH_URL` y `NEXT_PUBLIC_AUTH_BASE_URL`

Ejemplo `.env`:

```bash
BETTER_AUTH_SECRET=$(openssl rand -base64 32)
BETTER_AUTH_URL=http://localhost:3000
NEXT_PUBLIC_AUTH_BASE_URL=http://localhost:3000
```

---

## Problemas comunes

- `/zh` aparece en inglés: asegúrate de `localePrefix = "always"` y reinicia
- “Unknown module type” para MDX: verifica el plugin de Fumadocs y reinicia (log `[MDX] types generated`)
- 404 en docs: coloca el archivo en `content/docs/<locale>/...` y usa el slug correcto
